<%= form_for @user_artifact, :html => { :class => "form-horizontal user_artifact" } do |f| %>

  <% if @user_artifact.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@user_artifact.errors.count, "error") %> prohibited this user_artifact from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @user_artifact.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :user_id, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :user_id, :class => 'form-control' %>
    </div>
    <%=f.error_span(:user_id) %>
  </div>
  <div class="form-group">
    <%= f.label :artifact_id, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :artifact_id, :class => 'form-control' %>
    </div>
    <%=f.error_span(:artifact_id) %>
  </div>

  <div class="form-group">
    <div class="col-lg-offset-2 col-lg-10">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                user_artifacts_path, :class => 'btn btn-default' %>
    </div>
  </div>

<% end %>
